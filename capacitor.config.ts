import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Study-Manager',
  webDir: 'www',
  ios: {
    backgroundColor: '#000000'
  },
  android: {
    backgroundColor: '#000000'
  }
};

export default config;
